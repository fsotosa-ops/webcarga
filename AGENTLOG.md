# CLAUDE CONTEXT MEMORY
> Proyecto: webcarga
> Histórico completo en AGENTLOG_ARCHIVE.md — no es el histórico completo.

### 2026-07-16 (cont.) — Checkpoint H3 fase 2 completa: Empresas + Seguros (Checkpoints M/H0/H1/H2/H3) queda 100% cerrado

**Objetivo de esta sesión:** retomar donde la sesión anterior dejó el frontend a propósito roto (`npm run build` fallando con `Module not found` en ~24 componentes/2 páginas de Empresas/Seguros, ver AGENTLOG_ARCHIVE.md) y terminar la reescritura contra el modelo `public.*`/H2.

**Dos gaps reales de arquitectura encontrados antes de escribir componentes — ambos resueltos con el usuario, no decididos en solitario:**

1. **`DriverDetailPanel`/`VehicleDetailPanel` no tenían de dónde traer el checklist por conductor/vehículo**: `GET /drivers/{id}` y `GET /assets/{id}` solo devuelven el agregado (`total_requirements`), no la lista itemizada — a diferencia de `GET /carriers/{id}}`, que sí trae `compliance_records` anidado. El modelo ya soporta DRIVER/ASSET (es polimórfico), solo faltaba el endpoint de listado. **Resuelto**: agregados `GET /drivers/{id}/compliance-records` y `GET /assets/{id}/compliance-records` (mismo query que `_assemble_carrier_detail` en `carriers.py`, filtrado por `entity_type`) + tests + `lib/api/drivers.ts`/`assets.ts`. Suite backend: **128 passed** (de 125).
2. **`/dashboard/seguros` (Pólizas + Cobranza global, cross-empresa) no tiene backend**: el router nuevo de H2.3 solo expone `GET /carriers/{id}/policies` y `GET /policies/{id}/installments` (por póliza) — no hay `GET /policies` ni `GET /installments` globales. **Decisión del usuario**: retirar la ruta global por completo, remitiéndose al modelo lógico ya rediseñado — Seguros pasa a vivir *solo* anidado en la ficha de cada empresa. Borrados `app/dashboard/seguros/page.tsx`, `PolizasTab`/`CobranzaTab`/`InsuranceCompanyCard` (+tests), `lib/utils/insuranceFilters.ts`/`insuranceGrouping.ts` (+tests), link "Seguros" del `Sidebar.tsx`. La vista de Cobranza (aging, agrupación, botón "Pagar" real) queda sin equivalente — ver checklist abajo, no bloqueante para este workstream (ya estaba en pendientes de sesiones previas).

**Rediseños de fondo (no simple renombrado de tipos) que vale la pena recordar:**

- **Checklist de documentos data-driven de verdad**: `DocumentChecklist`/`ChecklistItem` pasó de un catálogo hardcodeado + JSONB `governance` (5 estados `ok/pendiente/actualizar/n_a/factible`) a mapear 1:1 sobre `ComplianceRecord` (7 estados reales del CHECK constraint). El backend ya calcula `is_expired`/`is_expiring_soon` por record — se borró todo el date-math client-side (`getAlertStatus`/`getDriverAlertStatus`/`getVehicleAlertStatus` en `lib/compliance.ts`). Nuevo `lib/utils/complianceChecklist.ts` reemplaza `transporterDocs.ts` (borrado, junto con `EligibilityDot`/`lib/utils/eligibility.ts` — el semáforo de elegibilidad de Checkpoint A-E no tiene equivalente en el modelo nuevo).
- **Roster cards (`DriverRosterCard`/`VehicleRosterCard`) simplificados a propósito**: `app.carrier_driver_roster`/`carrier_asset_roster` solo traen `total_requirements` agregado, no un desglose por estado — no hay semáforo posible a ese nivel sin ampliar la vista (fuera de alcance). Roster muestra conteo + fecha, el detalle por documento vive en el panel.
- **Baja/reactivar sin modal de motivo**: el modelo nuevo no tiene columna de "motivo de baja" en ninguna tabla (`carriers`/`drivers`/`assets`), solo `operational_status`. `BajaReasonModal` pasó de formulario (motivo + notas) a confirmación simple; baja/reactivar de conductor/equipo es un `PATCH operational_status` directo, ya no un endpoint `.../deactivate` separado.
- **`InsurancePolicyModal` reescrito con selección M:N real** (el pedido explícito del plan): coberturas (`coverageTypesApi` + `policiesApi.linkCoverage/unlinkCoverage`) y activos cubiertos (`carriersApi.listAssets` + `policiesApi.linkAsset/unlinkAsset`) como chips removibles + selector para agregar. Sin checklist de "documentos" por póliza (no existe en el modelo nuevo, solo `policy_document_url`/`endorsement_document_url` de solo lectura, sin endpoint de escritura). `InstallmentRow`: "revertir" pasó a ser el mismo `PATCH payment_status='PENDING'` (no hay endpoint de revert ni optimistic lock en el backend nuevo).
- **`ComplianceBadge` no se tocó de tipo**: en vez de acoplarlo al nuevo `ComplianceStatus`, se le dio un tipo local (`'ok'|'expiring_soon'|'expired'`) para no romper `TripTable.tsx` (Diario, fuera de alcance) que ya tenía su propio `AlertStatus` local desde H3 capa 1 — typing estructural, cero import cruzado entre módulos.
- **Página de empresa**: convertida a TanStack Query de punta a punta (antes era `useState`+`load()` manual). Tabs Activas/Legacy sobre `operational_status` (plan H3.4). Contactos dejaron de ser 4 slots fijos por rol (`rep_legal/operacional/finanzas/documentos`) — `public.contacts` es una lista abierta (`contact_role` sin CHECK constraint), la sección ahora es una lista con alta/edición/baja real.

**Verificación real, no solo asumida:**
- `npx tsc --noEmit` → **0 errores** (venía de 169 al empezar esta sesión).
- `npx vitest run` → **284 passed** (40 archivos).
- `npm run build` → **exitoso**, las 14 rutas compilan (el error de Vercel de la sesión anterior, `Module not found`, ya no se reproduce).
- `pytest` backend → **128 passed**.

**Commit + push hechos** (a pedido explícito del usuario, misma sesión): `70460be` en `dev`, pusheado a `origin/dev` (`38aae77..70460be`). 64 archivos, staging manual (no `git add -A`) para excluir explícitamente las ~70 migraciones de Checkpoints A-E ya marcadas para borrar (deletion pendiente de una sesión anterior, no tocada), `.pyc`/`__pycache__`/`venv/`, y varios archivos sueltos sin relación que ya estaban untracked en el working directory antes de esta sesión (`.mcp.json`, `auto_claude.sh`, `node_modules/`, etc.). `AGENTLOG_ARCHIVE.md` queda gitignored, no se commitea.

**Dado de baja (2026-07-16):** mapeo `doc_code`↔cliente (Fabián) — el usuario confirmó que ya no aplica, se retira de los pendientes.

### 2026-07-16 (cont.) — QA real del usuario contra el deploy: 5 gaps encontrados y corregidos, todos verificados con datos reales de Supabase

**Objetivo:** el usuario probó la app recién desplegada y reportó 5 problemas concretos. Se investigó cada uno contra Supabase real (`viclzoftiudkepqnhekv`) antes de tocar código — confirmó que ninguno era percepción, los 5 eran reales:

1. **Bug de conteo real**: Activas 12 + Legacy 88 = 100 (el header decía 246). Causa: los contadores de tabs se calculaban sobre una sola página de 100 filas (`LIMIT` del backend), no sobre el total. Reales: ACTIVE=38, LEGACY_INACTIVE=208. **Fix**: `app/dashboard/transportistas/page.tsx` reescrito — filtro `operational_status` server-side por tab + dos queries de conteo puro (`limit=1`, solo leen `count`) + paginación real (Legacy con 208 filas supera el límite de 100/página del backend, hacía falta sí o sí). `lib/utils/transporterFilters.ts` quedó huérfano, borrado.
2. **"No tengo opción de subir archivos en Empresas"**: causa raíz real, no un bug de UI — **35 de 36 requisitos del catálogo real tenían `requires_file=false`**, incluyendo cosas obviamente documentales (Licencia de Conducir, Padrón, F30 Multas, Copia C.I.). Mi UI gatea el botón de subir correctamente detrás de `requires_file`; el catálogo estaba mal cargado. **Fix, confirmado con el usuario antes de tocar producción**: migración `h3_fix_requires_file_catalog` — `UPDATE compliance_requirements SET requires_file = true WHERE requires_file = false` (36/36 ahora `true`). Solo metadata del catálogo, no toca `compliance_records` existentes.
3. **`legacy_admin_id` no se veía en la ficha**: ya venía en `Carrier.legacy_admin_id`, solo faltaba renderizarlo — fix de una línea en la página de detalle.
4. **Generador de carga (Walmart/Colun/Sodimac) no se veía**: `public.carrier_shippers` tiene datos reales pero no había endpoint. Agregado `GET /carriers/{id}/shippers` + `carriersApi.listShippers` + chips en la ficha.
5. **Gap de Seguros (de la sesión anterior)**: agregado `POST /policies/{id}/file?kind=document|endorsement` (mismo patrón que compliance-records) + expuesto `missing_physical_file` (la vista `app.carrier_insurance_status` ya lo calculaba, nunca se seleccionaba) + UI de subida en `InsurancePolicyModal`.

**Hallazgo adicional no pedido, pero en el mismo área**: `file_url` en `compliance_records` (y ahora en `insurance_policies`) guardaba el **storage_path crudo**, no una URL usable — el bucket `compliance-docs` no es público. Los links "Ver archivo" habrían quedado rotos para cualquier documento subido de verdad. Nuevo helper `resolve_signed_url()` en `document_storage.py`, aplicado en `GET /carriers/{id}`, `GET/PATCH /compliance-records/{id}`, `GET /drivers|assets/{id}/compliance-records`, y `GET/PATCH /policies/{id}`. De paso se blindaron los tests de `test_carriers.py`/`test_drivers.py`/`test_assets.py` con override de `get_supabase` (antes construían un cliente Supabase real con credenciales reales del `.env`, sin llegar a usarlo solo porque ninguna fixture tenía `file_url` no-nulo — riesgo latente cerrado).

**Verificación real, incluyendo navegador en vivo (Playwright contra el deploy real, no local)**: `tsc --noEmit` limpio, `vitest` 288/288, `npm run build` exitoso, `pytest` **135 passed**. Se navegó `https://webcarga-frontend-dev-zcdyyci7ta-uc.a.run.app` autenticado como owner real — confirmado que Conductores/Equipos con datos reales SÍ se renderizaban correctamente desde el primer commit de la sesión (la duda del usuario sobre esto era sobre una carga que ya estaba resuelta), y que el fix de `requires_file` (cambio de datos, sin necesitar deploy) ya mostraba los botones "Subir archivo" en vivo. **Hallazgo aparte de performance**: los ~12 links "Ver ficha completa" del listado disparan prefetch de Next.js simultáneo, el backend responde `429` (rate limit Upstash) — no rompe la navegación (el click hace su propio fetch), pero es ruido real en consola, sin decidir si se ataca.

**Commit + push**: `94e395e` en `dev`/`origin/dev`. Verificado en vivo con Playwright autenticado contra `https://webcarga-frontend-dev-zcdyyci7ta-uc.a.run.app` tras el deploy: Conductores/Equipos, `legacy_admin_id`, chips de generador de carga y botones de subir archivo — los 5 gaps confirmados resueltos en producción, no solo en tests.

### 2026-07-16 (cont.) — Rediseño SaaS de la ficha de Empresas + modal de Seguros (pedido explícito: "muy pesada", "alertas saturadas", "mucho scroll y click")

**Decisión de layout**: en vez de preguntar tabs-vs-sidebar (la pregunta se cortó dos veces sin respuesta), el usuario pidió directamente el estándar de industria para este patrón — compliance/document tracking B2B SaaS (Stripe customer pages, Vanta/Drata). Convergen en: header persistente + tabs horizontales + resumen compacto (score/ring, no un dump de la lista completa). Se implementó eso sin más vueltas.

- **Ficha de empresa** (`app/dashboard/transportistas/empresa/[id]/page.tsx`): reestructurada en tabs **Resumen / Documentos / Conductores / Equipos / Seguros / Contactos** — una sola sección visible a la vez, cero scroll entre ellas. Badges de conteo por tab (Documentos en rojo si hay pendientes obligatorios). El banner de alerta que listaba los 12+ documentos faltantes se retiró del nivel superior — Resumen ahora muestra un `CompletionRing` (ya existente, reusado) + top 3 problemas + accesos directos a Conductores/Equipos, el detalle completo vive solo dentro de la tab Documentos.
- **Modal de póliza** (`InsurancePolicyModal.tsx`): no ameritaba tabs completos (ya está scoped a una sola póliza), pero apilaba 4 secciones verticalmente pese a tener ancho de sobra. Reorganizado en grid de 2 columnas (Documentos+Enlaces / Coberturas+Activos cubiertos), cuotas se queda full-width abajo por ser una lista que puede crecer.

**Verificado real, no solo tests**: `tsc --noEmit` limpio, `vitest` 291/291 (tests de la ficha reescritos para navegar tabs antes de aserciones — nuevo helper `clickTab()` que desambigua el botón de la tab bar del stat-card de Resumen que comparte texto accesible), `npm run build` exitoso. **Playwright contra el deploy real** tras cada push (`e342f83` ficha, `2aac58b` modal): navegado Dankar Logistic Spa e Inversiones Casilla Spa (2 pólizas reales, cuotas vencidas reales) — capturas confirman el layout funcionando con datos de producción, incluido el flujo completo de "Pagar" sobre una cuota vencida real.

**Nota de intento fallido**: se intentó levantar el frontend local (`npm run dev`) para verificar antes de pushear, pero el backend local no respondió (`fetch failed`) — problema de config de entorno (`.env.local`/`FASTAPI_URL`), no del código. Se pusheó confiando en la suite automática y se verificó después contra el deploy real, patrón que ya viene funcionando bien esta sesión.

### 2026-07-16 (cont.) — Segunda ronda SaaS: alertas con tiempo relativo + landing de Seguros sincronizada con Empresas

**Pedido del usuario** (verbatim): *"no hay un modulo de seguros en el sidebar. es modulo de seguros no deberia estar sincronizado con lo que aparece en empresas? En uno de los btw mencionaste que faltan vistas materializadas para que el seguimiento de las alertas sean mas especificas. y un bloque grande rojo y estatico se ve sucio y para nada worldclass... agrega a este requerimiento el rediseno del listado de empresas"*. Dos decisiones clarificadas con el usuario antes de tocar código (AskUserQuestion, no asumidas):
1. El módulo Seguros del sidebar = **"landing sincronizada con Empresas"**: listado de carriers ordenado/filtrable por salud de seguro agregada, misma fuente real que ya se ve en la tab Seguros de la ficha (`app.carrier_insurance_status`) — **no** se restaura la vista global de Cobranza/cuotas cross-empresa (retirada explícitamente en H3 fase 2, sigue fuera de alcance).
2. "Vistas materializadas para trazabilidad" = el usuario aclaró que no pedía campos de negocio nuevos por documento, sino **especificidad de tiempo relativo** en las alertas ("vence en X días" / "vencido hace X días" / "sin actualizar hace X días") en vez de alertas binarias.

**Trabajo hecho:**
- **Backend — `updated_at` en `compliance_records` anidados**: agregado a los 3 queries que lo devuelven (`carriers.py`, `drivers.py`, `assets.py`).
- **Frontend — helpers de tiempo relativo** (`lib/compliance.ts`): `expiryRelative()` y `updatedRelative()`, con fallback (si no hay `expiration_date` usable, cae a "hace cuánto no se actualiza"). Aplicados en `TransporterAlertBanner` (reescrito de bloque rojo estático a card blanca con filas y pills de color por status real) y en el top-3 de problemas de la tab Resumen de la ficha.
- **Backend — `GET /carriers/insurance-overview`** (`routers/carriers.py`, antes de `/{carrier_id}` para no colisionar): agrega pólizas por carrier desde `app.carrier_insurance_status` — `total_policies`, `total_overdue_installments`, `next_payment_date`, `worst_policy_health` (peor entre EXPIRED > EXPIRING_SOON > VALID > CANCELLED, `NULL` si no tiene pólizas), ordenado por urgencia. Soporta `q` (nombre/tax_id) y `health` (filtro exacto por bucket, incluyendo `NONE` = sin pólizas) server-side — necesario porque son ~246 carriers y el límite de página es 100, un filtro solo client-side habría escondido resultados fuera de la primera página. Devuelve además `facets` (conteo por bucket sobre el universo filtrado por `q`, sin aplicar `health` — así los conteos de los otros tabs no cambian al clickear uno). Nuevo schema `CarrierInsuranceOverviewResponse`/`InsuranceOverviewFacets` en `schemas/insurance.py`. 5 tests nuevos en `test_carriers.py` (agregación, búsqueda, filtro por health, filtro `NONE`, 422 en health inválido). Suite backend: **140 passed** (de 135).
- **Frontend — landing `/dashboard/seguros`** (`app/dashboard/seguros/page.tsx`, nueva): tabs Todas/Vencidas/Por vencer/Al día/Sin póliza con conteos de `facets`, búsqueda, tabla (empresa, badge de salud, # pólizas, cuotas vencidas, próximo pago con tiempo relativo vía `dueRelative()`), paginación. Clic en una fila abre el mismo `InsurancePolicyModal` ya usado en la ficha de empresa (reuso real, no duplicado) — maneja bien el caso "sin pólizas" (ya tenía ese estado). Extraído `POLICY_HEALTH_CONFIG` de `InsurancePolicyModal.tsx` a `lib/insurance.tsx` (nuevo) para no duplicar el badge de salud entre la ficha y la landing.
- **Sidebar**: agregado ítem "Seguros" (icono `Shield`, ya importado) a `NAV_ITEMS` — aparece tanto en el nav desktop como en el bottom nav mobile (mismo array).

**Verificado real**: `pytest` **140 passed**, `npx tsc --noEmit` limpio, `npx vitest run` **303 passed** (40 archivos), `npm run build` exitoso con `/dashboard/seguros` listada entre las rutas dinámicas. Pendiente: verificación en vivo con Playwright contra el deploy tras el push (no hecha todavía en este punto de la sesión).

**Push + verificación en vivo, incluido un bug real reportado por el usuario a mitad de sesión**: tras pushear `insurance-overview`/landing Seguros/Sidebar (`8a56ebf`), el usuario reportó en vivo *"hay un bug que si me muevo muy rapido entre modulos de la app me aparece un bug 'Too Many Requests'"*. Reproducido de inmediato con Playwright contra el deploy. Dos causas reales, no una:
1. **El sliding window de Upstash era demasiado bajo para uso normal**: `proxy.ts` limitaba a 20 req/10s por IP compartidas entre `/dashboard` (la página en sí) y todas las llamadas paralelas a `/api/v1/...` que esa página dispara (la ficha de empresa sola hace 5 al abrir: carrier + drivers + assets + policies + shippers) — 3-4 clicks entre módulos ya alcanzaban el límite para un solo usuario. Subido a 60/10s (`1f64d72`).
2. **Causa raíz más determinante, ya conocida pero sin resolver desde sesiones anteriores**: con 30+ tarjetas visibles en `/dashboard/transportistas`, Next.js **prefetchea automáticamente** todos los links "Ver ficha completa" en viewport al renderizar la página — eso solo agotaba el budget del rate limiter antes de que el usuario clickeara nada, reproducible incluso con el límite ya subido a 60. Fix: `prefetch={false}` en esos links (`TransporterCard.tsx` y la vista tabla de `page.tsx`) — `480b773`.

**Verificado en vivo, no solo asumido**: con el fix de `prefetch=false` desplegado, se repitió la secuencia exacta que había reproducido el 429 (cargar listado de Empresas → entrar a una ficha inmediatamente, dos veces, más 7 navegaciones consecutivas sin pausa entre Diario/Empresas/Seguros/ficha) — cero errores 429, `browser_console_messages` limpio en cada paso. De paso se corrigió que la landing de Seguros no era responsive en mobile (tabla de 6 columnas se salía del viewport sin scroll) — ahora cards en `md:hidden`, tabla completa desde `md:` (mismo patrón que `TransporterCard`), `25ee8d5`.

#### Próximo paso exacto
1. [ ] Rediseño SaaS pendiente: el **listado** de Empresas (`/dashboard/transportistas`, tarjetas/tabla + paginación) sigue sin la pasada visual (Task 21) — el pedido explícito del usuario lo incluye ("agrega a este requerimiento el rediseno del listado de empresas"), siguiente paso inmediato.
2. [ ] H2.6 (decisión pendiente, sigue sin resolver desde Checkpoint M): si/cómo el módulo del Diario debe mostrar compliance/seguro del carrier — condiciona reactivar `EmpresaSelector`/`TransporterAssignSection`/las alertas de `TripTable`. **No iniciar sin que el usuario lo pida explícitamente.**
3. [ ] Cobranza (aging/agrupación cross-empresa, botón "Pagar" real) sigue sin ruta dedicada — la nueva landing de Seguros es un resumen agregado por carrier, no reemplaza una vista de cuotas individuales cross-empresa; requeriría endpoints nuevos (`GET /policies`, `GET /installments` globales), fuera de alcance por ahora.
4. [ ] Datos estructurados por documento (ej. F30: monto de la multa, fecha) más allá del archivo — pedido original del usuario, la clarificación de esta sesión resolvió la parte de "trazabilidad de alertas" (tiempo relativo) pero **no** confirmó si el usuario todavía quiere campos de negocio estructurados por tipo de documento. Preguntar antes de construir nada.
5. [ ] Pendientes de sesiones anteriores (no bloqueantes): decidir qué hacer con las ~70 migraciones de Checkpoints A-E ya marcadas para borrar (deletion pendiente en el working tree, sin commitear).

---

### 2026-07-17 — Tercera ronda: interactividad Seguros↔Empresas, corrección de rumbo en modales, tabs de alertas, formulario de póliza completo

**Contexto**: continuación directa de la ronda anterior. El usuario pidió tres cosas en paralelo: (1) que la tarjeta de Seguros en la ficha de empresa fuera clickeable hacia el módulo Seguros, (2) el estándar de industria para el tamaño de los modales de detalle, (3) tabs de alertas en el listado de Empresas. En el camino aparecieron dos gaps reales no pedidos explícitamente que se corrigieron igual: una empresa sin pólizas no tenía forma de agregar la primera, y el formulario de alta de póliza (cuando se agregó) solo pedía 2 de los 5 campos que el modelo de datos real soporta.

**Corrección de rumbo importante — modales**: la primera pasada interpretó "los modales no deberían ser wide-screen" como "hacerlos angostos" y los convertí a slide-over lateral de 640px (`8734f49`). El usuario aclaró que quería lo contrario ("cuando me referia a los modales pensaba en full-widescreen") y pidió el estándar worldclass — usé `AskUserQuestion` (no adiviné una segunda vez) ofreciendo 3 patrones reales (diálogo grande centrado tipo Airtable/Attio, full-screen real, o el slide-over ya construido). Eligió el diálogo grande centrado. `DriverDetailPanel`/`VehicleDetailPanel`/`InsurancePolicyModal` volvieron a diálogo centrado con rail izquierdo + contenido, pero agrandado (`w-[92vw] max-w-6xl h-[85vh]` en vez del `max-w-4xl` original) — `ba62373`. **Lección**: cuando ya erré una vez interpretando una instrucción de diseño ambigua, la segunda vez se pregunta explícitamente en vez de re-adivinar.

**Gap real encontrado — empresa sin pólizas era un callejón sin salida**: el usuario probó con tax_id `77094744-8` (Comercializadora De Los Rios Ltda, real, sin pólizas) y el modal de Seguros solo mostraba "Sin pólizas registradas", sin acción posible. Agregado un CTA "+ Agregar la primera póliza" con formulario inline en el mismo modal, reusando `POST /carriers/{id}/policies` (mismo patrón que ya existía en la tab Seguros de la ficha, ahora con feature-parity real entre ambos puntos de entrada).

**Gap real encontrado — el formulario de alta de póliza no seguía el modelo de datos real**: el usuario lo señaló directamente ("no sigue el modelo lógico ni de datos... el modelo lógico y datos existe en la db y responde a inserciones y updates"). El formulario (tanto el nuevo del modal como el viejo de la ficha) solo pedía `insurance_company`/`policy_number`, dejando `valid_from`/`valid_to`/`expiration_alert_days` sin setear pese a que `InsurancePolicyCreateBody` los soporta — una póliza recién creada quedaba con menos datos que el resto. Extraído `PolicyCreateForm` (componente compartido, `compact` prop para el sidebar vs. el estado vacío) con los 5 campos reales, reusado en ambos puntos de entrada.

**Tabs de alertas en Empresas (pedido explícito, mismo patrón que Seguros)**: `GET /carriers` gana `health=PENDING|OK` + `facets`, computado en vivo sobre `public.compliance_records`/`compliance_requirements` (mismo criterio de "problema obligatorio" que `mandatoryProblems` en la ficha — LEGAL_MANDATORY con status MISSING/EXPIRED/REJECTED o vencido por fecha). Deliberadamente **no** se tocó `app.carrier_compliance_status` (la vista materializada que ya alimentaba `total_requirements`/`last_document_update`) — el join de severidad es una consulta en vivo aparte, sin migración. `TransporterCard` y la vista Tabla reemplazan el conteo plano "N requisitos" (igual para casi todas las empresas, sin señal real) por un pill rojo/verde con el conteo de pendientes — mismo lenguaje visual que ya tenía Seguros. **Hallazgo real via Playwright contra el deploy**: las 38 empresas Activas tienen actualmente 12 documentos obligatorios pendientes cada una (Pendientes 38, Al día 0) — no es un bug, es el estado real del catálogo de compliance todavía sin aprobar masivamente.

**Seguros↔Empresas interactivo**: `InsuranceSummaryCard` (usado en la ficha y en el slide-over de Empresas) pasó de card estática a `Link` hacia `/dashboard/seguros?q=<tax_id>` — deep link real, verificado en vivo (navegar desde la ficha de Dankar Logistic Spa aterriza en la landing de Seguros con esa única empresa filtrada). La landing lee `?q=` inicial vía `useSearchParams` (envuelto en `Suspense`, requerido por Next.js).

**Verificado real, no solo tests**: `pytest` 142 passed, `tsc --noEmit` limpio, `vitest` 307 passed, `npm run build` exitoso. Playwright contra el deploy real tras cada push: tabs de alertas con datos reales, modal grande centrado con rail+contenido, formulario de póliza con los 5 campos, deep link `?q=` funcionando end-to-end. Consola limpia en cada verificación.

**Continuación misma sesión — más gaps reales encontrados por el usuario probando en vivo, todos con la misma raíz: "el backend ya lo soporta, la UI nunca lo expuso":**

1. **`TransporterSlideOver` quedó como el único modal sin convertir**: el usuario lo notó ("el form lateral al seleccionar una empresa... rompe con la UX/UI de toda la app"). Convertido al mismo patrón de diálogo grande centrado (rail identidad+`CompletionRing`+"Ver ficha completa" / contenido documentos+contactos+seguros) — `d159deb`.
2. **`has_endorsement` (público.insurance_policies) nunca era seteable**: columna real con default `false`, pero ni `InsurancePolicyCreateBody` ni `InsurancePolicyPatchBody` la exponían — la fila "Endoso" del checklist de documentos era permanentemente inalcanzable para cualquier póliza. Agregado a ambos schemas + checkbox "Tiene endoso" en `PolicyCreateForm` + botón "Esta póliza tiene endoso" para pólizas existentes sin uno (toggle vía PATCH).
3. **No existía forma de generar cuotas**: `public.insurance_installments` solo se llenaba por import/migración — había PATCH para marcar una cuota existente como pagada, pero ningún POST para crear el plan. Nuevo `POST /policies/{id}/installments/generate` (mensual, monto fijo, valida que la póliza no tenga cuotas ya) + UI "Generar plan de cuotas" en el estado vacío de la sección Cuotas.
4. **`compliance_records.expiration_date` nunca era editable en `DocumentChecklist`** (usada en conductores/vehículos) ni en `TransporterDocumentsPanel` (empresa): el backend ya soporta `PATCH .../expiration_date` desde hace tiempo, pero ninguna de las dos UIs mostraba ni un input de fecha — cédula, licencia, revisión técnica, SOAP, permiso de circulación no tenían forma de monitorear vencimiento pese a que el modelo de datos ya lo soporta. Agregado input de fecha + caption de tiempo relativo (`expiryRelative`) en ambos componentes.

**Aún en curso, mismo hallazgo** (ver checklist abajo): alertas de compliance a nivel de roster de Conductores/Equipos (mismo patrón que se aplicó a Empresas), y año del vehículo (`public.assets` no tiene columna `year` en ninguna migración — requiere `ALTER TABLE`, no solo exponer un campo ya existente).

**Verificado real**: `pytest` 147 passed, `tsc --noEmit` limpio, `vitest` 314 passed, `npm run build` exitoso.

#### Próximo paso exacto
1. [ ] **Alertas de compliance a nivel de Conductores/Equipos** (pedido explícito: "las alertas en el caso de las empresas tambien deberian estar a nivel de conductores y vehiculos") — `DriverRosterCard`/`VehicleRosterCard` siguen mostrando `total_requirements` plano, mismo gap que tenía `TransporterCard` antes del fix de esta sesión. Requiere extender `GET /carriers/{id}/drivers` y `GET /carriers/{id}/assets` con el mismo join en vivo de severidad usado en `GET /carriers`.
2. [ ] **Año del vehículo** (pedido explícito) — `public.assets` no tiene columna `year`/`manufacture_year` en ninguna migración (verificado grep completo). Requiere migración `ALTER TABLE public.assets ADD COLUMN manufacture_year INT` (aditiva, nullable, bajo riesgo) + exponer en `Asset`/`AssetPatchBody`/`VehicleDetailPanel`.
3. [ ] Datos estructurados por documento (ej. F30: monto de la multa, fecha) — sigue sin confirmar si el usuario todavía lo quiere más allá de lo ya resuelto. Preguntar antes de construir nada.
4. [ ] H2.6 (decisión pendiente, sigue sin resolver desde Checkpoint M): si/cómo el módulo del Diario debe mostrar compliance/seguro del carrier. **No iniciar sin que el usuario lo pida explícitamente.**
5. [ ] Cobranza (aging/agrupación cross-empresa, botón "Pagar" real) sigue sin ruta dedicada — fuera de alcance por ahora.
6. [ ] Pendientes de sesiones anteriores (no bloqueantes): decidir qué hacer con las ~70 migraciones de Checkpoints A-E ya marcadas para borrar (deletion pendiente en el working tree, sin commitear).

---
