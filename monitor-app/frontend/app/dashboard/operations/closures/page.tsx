/** Placeholder temporal — esta ruta va a alojar el "Centro de Cierre del
 *  Día" unificado (tarea futura). La Reportería histórica que vivía acá se
 *  reubicó a /dashboard/operations/closures/history. */
export default function ClosuresCenterPlaceholderPage() {
  return (
    <div className="p-6">
      <p className="text-sm text-gray-400">En construcción</p>
    </div>
  )
}
