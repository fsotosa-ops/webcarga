# Technical Debt — webcarga

Lista de deuda técnica priorizada. Actualizar con `/debt-log`.

| Prioridad | Item | Archivo | Fecha |
|-----------|------|---------|-------|
| Media | `app.trip_stops.local` es texto libre sin FK real a `public.locations` — 320 nombres distintos de parada usados en viajes reales, 305 (95%) calzan exacto (case/trim-insensitive) con un nombre en `public.locations` (700 locales activos), ~15 no calzan con nada del catálogo. Sin FK no hay garantía de alineación futura ni señal cuando un nombre no calza (drift silencioso). Decisión del usuario (2026-07-25): dejarlo pendiente para conversar primero con el equipo de negocio antes de diseñar cualquier fix — rango de opciones va desde solo superficiar los nombres sin calzar como señal de calidad de dato, hasta migrar `trip_stops.local` a un `location_id` real con flujo de resolución tipo fleet-match. No ejecutar ningún cambio de modelo hasta esa conversación. | `app.trip_stops` / `public.locations` | 2026-07-25 |
| Baja | `PATCH /trips/bulk-close` y `tripsApi.bulkClose` quedaron sin consumidor tras retirar el checkbox de cierre masivo de `TripTable` (Centro de Cierre unificado, plan 1.6) — el cierre de viajes ahora es siempre consecuencia del flujo estructurado (`/dashboard/operations/closures`), no una acción suelta sobre la sábana. Tiene su propia suite (`tests/test_trip_bulk_close.py`) que quedaría huérfana también. No se borró por riesgo de que algún flujo externo/script lo use sin que este plan lo haya cubierto — candidato a limpieza en una sesión futura, con confirmación explícita del usuario antes de borrar. | `backend/api/app/routers/trips.py` / `frontend/lib/api/trips.ts` | 2026-08-04 |
| Baja | `equipmentClosuresApi.get()` (frontend) y su backend `GET /equipment-closures` quedaron sin consumidor tras borrar `EquipoCompletoClosureSection.tsx` en la unificación de "Flota del día" — `FlotaDelDiaSection` ahora lee Equipo Completo vía `tripsApi.fleetDailyOverview`, no `equipmentClosuresApi.get`. `equipmentClosuresApi.close()` sigue en uso (botón "Confirmar cierre"). No se borra en esta ronda: requiere confirmar que ningún script/flujo externo lo llame antes de eliminar el endpoint y su suite de tests. | `frontend/lib/api/equipmentClosures.ts` / `backend/api/app/routers/equipment_closures.py` | 2026-08-04 |
