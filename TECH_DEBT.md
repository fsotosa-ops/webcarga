# Technical Debt — webcarga

Lista de deuda técnica priorizada. Actualizar con `/debt-log`.

| Prioridad | Item | Archivo | Fecha |
|-----------|------|---------|-------|
| Media | `app.trip_stops.local` es texto libre sin FK real a `public.locations` — 320 nombres distintos de parada usados en viajes reales, 305 (95%) calzan exacto (case/trim-insensitive) con un nombre en `public.locations` (700 locales activos), ~15 no calzan con nada del catálogo. Sin FK no hay garantía de alineación futura ni señal cuando un nombre no calza (drift silencioso). Decisión del usuario (2026-07-25): dejarlo pendiente para conversar primero con el equipo de negocio antes de diseñar cualquier fix — rango de opciones va desde solo superficiar los nombres sin calzar como señal de calidad de dato, hasta migrar `trip_stops.local` a un `location_id` real con flujo de resolución tipo fleet-match. No ejecutar ningún cambio de modelo hasta esa conversación. | `app.trip_stops` / `public.locations` | 2026-07-25 |
| Resuelto | ~~`PATCH /trips/bulk-close` y `tripsApi.bulkClose` quedaron sin consumidor~~ — el paso "Viajes" del Cierre del Día (`PasoViajesSection.tsx`, plan `2026-08-18-cierre-paso-viajes`) lo estrenó: es el mecanismo con el que operaciones declara "No asignado por WebCarga" en lote, con motivo obligatorio. `tests/test_trip_bulk_close.py` dejó de estar huérfana. | `backend/api/app/routers/trips.py` / `frontend/lib/api/trips.ts` | 2026-08-18 |
| Baja | `tripsApi.fleetDailyOverview()`/`GET /trips/fleet-daily-overview` quedaron sin consumidor: `FlotaDelDiaSection` pasó de leer Equipo Completo desde ahí a leer `equipmentClosuresApi.get()` (paridad de conductor/motivo/acción con Tractoreo, pedido explícito del usuario). `FleetOverviewEquipment`/`FleetOverviewCategory`/`FleetDailyOverviewResponse` (frontend) también quedan sin consumidor real. No se borra en esta ronda: mismo criterio que el resto de esta tabla, confirmar antes que nada externo lo use. | `backend/api/app/routers/trips.py:1268` / `frontend/lib/api/trips.ts` | 2026-08-04 |
| Media | `test_asignar_conductor.py::test_se_puede_vincular_un_conductor_sin_empresa` fija un invariante que dejó de ser cierto: afirma `carrier_id is None` tras vincular sólo al conductor. El 2026-08-23 la migración `20260823210000_el_link_manual_deja_de_quedarse_sin_empresa.sql` cambió eso **a propósito** — el arreglo del reclamo de Pablo sobre Edgar. Cadena verificada: el endpoint inserta el link con `carrier_id` nulo y después hace `UPDATE app.trips SET fleet_link_id`, que dispara `trg_trips_resolve_fleet_upd` → `app.resolve_trip_fleet()`, cuyo UPDATE final rellena la empresa desde `driver_assignments`. El UUID del fallo (`a74501a1…`) es exactamente la empresa del conductor que el test elige. **No es un bug del código**: el arreglo funcionó, de 25 links manuales hoy hay 0 sin empresa (eran 14 el 23/08). Lo que falta es actualizar la aserción para que fije la regla vigente —la inferencia llena un silencio y nunca contradice: `carrier_id in (None, empresa_del_conductor)`— en vez de borrarla. Decisión del usuario (2026-08-25): dejarlo así por ahora y registrarlo como deuda. **Mientras tanto la suite queda en rojo (1 de 968), y una suite que siempre falla enseña a no mirarla.** **ACTUALIZACIÓN 2026-08-27: hoy PASA, y eso lo empeora.** El test elige un conductor de la base de producción, así que su resultado depende de si el conductor que le tocó tiene o no una asignación activa — y el 25 y 26/08 hubo desvinculaciones reales en esa tabla. Ya no es un test rojo conocido: es un test que cambia de color solo, sin que nadie toque el código. Verde hoy, rojo mañana, y nadie va a saber por qué. La corrección sigue siendo la misma (fijar la regla vigente, `carrier_id in (None, empresa_del_conductor)`) más elegir el conductor de datos que el propio test cree. | `backend/api/tests/test_asignar_conductor.py:64` | 2026-08-25 |
| Media | Un fallo de conexión en un test de integración se convierte en **salteado, no en rojo**. `conexion_revertida` (tests/conftest.py:206-209) atrapa `OSError`/`TimeoutError`/`PostgresError` y hace `pytest.skip(MOTIVO_SIN_RED)`. Eso está bien para "no hay credenciales" —una máquina sin acceso no debería fallar la suite— pero **no** para "no pude conectar": ahí el test no probó lo que dice probar, y la suite igual reporta verde. **Observado el 2026-08-27**: una corrida completa dio `968 passed, 19 skipped` mientras los 190 de integración por separado daban `190 passed, 0 skipped` y los 797 mockeados `797 passed, 0 skipped`. Una segunda corrida completa dio **`987 passed, 0 skipped`**: el hipo fue transitorio y no se reprodujo. Pero el mecanismo queda: cualquiera de los 190 puede saltearse en silencio, incluidos los que verifican lo que uno acaba de tocar. **Arreglo**: separar los dos motivos — `skip` sólo cuando faltan credenciales, y `fail` cuando la conexión se cae, con reintento si hace falta. | `backend/api/tests/conftest.py:206` | 2026-08-27 |
